# 配置示例

## 使用方法

将对应场景的配置复制到 `config/config.yaml` 中使用。

## 场景列表

- `config_with_comments.yaml` - 带详细注释的完整配置
- `example1_simple.yaml` - 简单训练脚本
- `example2_with_args.yaml` - 带参数的训练
- `example3_conda.yaml` - 使用 Conda 环境
- `example4_bash.yaml` - 使用 Shell 脚本
- `example5_hpc.yaml` - HPC 配置（推送通知）
